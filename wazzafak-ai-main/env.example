# ==============================================================================
# Wazzafak AI Interview Backend - Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your values

# ------------------------------------------------------------------------------
# Supabase Database Configuration - POOLER CONNECTION (Port 6543) - RECOMMENDED
# ------------------------------------------------------------------------------
# IMPORTANT: Using Connection Pooler to avoid prepared statement cache issues
# Get your Supabase connection string from:
# Supabase Dashboard > Settings > Database > Connection string > Connection pooling
# 
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
#
# Your Supabase Project: npeusanizvcyjwsgbhfn
# Password: Hamoudi123? (URL-encoded as Hamoudi123%3F)
# Region: eu-north-1
# 
# URL-encode special characters in password:
#   ? → %3F
#   @ → %40
#   # → %23
#
# For Docker Compose, set this environment variable:
DATABASE_URL=postgresql://postgres.npeusanizvcyjwsgbhfn:Hamoudi123%3F@aws-0-eu-north-1.pooler.supabase.com:6543/postgres?sslmode=require

# For local PostgreSQL (if not using Supabase):
# DATABASE_URL=postgres://admin:password@db:5432/interview_db?sslmode=disable

# For local PostgreSQL:
# DATABASE_URL=postgres://admin:password@localhost:5432/interview_db?sslmode=disable

# ------------------------------------------------------------------------------
# OpenAI / LLM Configuration
# ------------------------------------------------------------------------------
# Your OpenAI API key (required unless USE_MOCK=1)
LLM_API_KEY=sk-your-openai-api-key-here

# Optional: Custom base URL for Azure OpenAI or OpenRouter
# LLM_BASE_URL=https://api.openai.com/v1

# Model configuration
EMBEDDING_MODEL=text-embedding-3-small
CHAT_MODEL=gpt-4o-mini
EVAL_MODEL=gpt-4o

# ------------------------------------------------------------------------------
# Application Settings
# ------------------------------------------------------------------------------
# Server port (default: 8089)
PORT=8089

# Set to 1 for mock mode (no OpenAI calls - for testing)
USE_MOCK=0

# ==============================================================================
# Android SDK Configuration Notes
# ==============================================================================
# 
# For Android Emulator, use:
#   String backendUrl = "http://10.0.2.2:8089";
#
# For Physical Android devices on same network, use your local IP:
#   String backendUrl = "http://192.168.x.x:8089";
#
# Find your local IP with: ipconfig (Windows) or ifconfig (Mac/Linux)
# ==============================================================================

